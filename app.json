import React from "react";
import { SafeAreaView, View, Text, Button, Alert, StyleSheet, StatusBar } from "react-native";

export default function App() {
  const handlePress = () => {
    Alert.alert("¡Hola! 👋", "¡Botón presionado correctamente!");
  };

  return (
    <SafeAreaView style={styles.safe}>
      <StatusBar barStyle="dark-content" />
      <View style={styles.container}>
        <Text style={styles.title}>¡Mi primera app con React Native!</Text>

        {/* Flexbox: centrado y ancho controlado del botón */}
        <View style={styles.buttonWrapper}>
          <Button title="Presióname" onPress={handlePress} />
        </View>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: {
    flex: 1,
    backgroundColor: "#f4f4f4",
  },
  container: {
    flex: 1,
    padding: 16,
    justifyContent: "center",   // centra verticalmente
    alignItems: "center",        // centra horizontalmente
  },
  title: {
    fontSize: 22,
    fontWeight: "bold",
    marginBottom: 20,
    textAlign: "center",
  },
  buttonWrapper: {
    width: "70%",                // el botón ocupa 70% del ancho disponible
    alignSelf: "center",
  },
});
